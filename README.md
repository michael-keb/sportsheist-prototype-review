# SportsHeist Client Review Prototype

Interactive HTML prototype for screen-by-screen client review (comments + voice notes).

Open `/` or `/prototype.html`. Feedback is stored in the reviewer's browser (localStorage / IndexedDB).
