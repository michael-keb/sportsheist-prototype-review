services:
  - type: web
    name: sportsheist-prototype-review
    runtime: static
    buildCommand: "true"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /
        destination: /index.html
